# phase 3-3 v2 (2026-09-17 21:45, redesign after the 96-step under-fit): law-sft 3,200 + med B x2 (52,002 rows, as in the 4-domain
# warm-up) + tau2 16,384 rows (half an epoch of the tools-rendered tau2 SFT set; ~133M tokens, matching the med token share ~138M).
# 1 NODE (8 x 32k bins per step, ~1,075 optimizer steps ~ the 4-domain warm-up's 1,144). Same recipe otherwise (1 epoch, lr 1e-5 cosine).
# NOT submitted until the user confirms (replaces sft_warmup_mix5_lawsft_med_tau2 / job 577276 and the pending MOPD arm 577310).
model:
  base: models/Qwen3-4B-OT3
  attn: auto
data:
  sources: {math: null, code: null, if: null, mix: data/sft_warmup/mix_lawsft3200_medB2ep_tau16384.jsonl}
  cache_dir: null
  weights: null
  max_rows: null
  max_len: 32768
  thinking: true
  packing: flatten
  seed: 42
train:
  output: outputs/sft/sft_warmup_mix5_lawsft_med_tau2_v2
  epochs: 1.0
  max_steps: -1
  lr: 1.0e-5
  scheduler: cosine
  warmup_ratio: 0.05
  weight_decay: 0.0
  max_grad_norm: 1.0
  per_device_bs: 1
  grad_accum: 1
  liger: true
  grad_ckpt: true
  logging_steps: 1
  save_strategy: epoch
  save_total_limit: null
  save_only_model: true
  resume: auto
  seed: 42
  dataloader_workers: 2
