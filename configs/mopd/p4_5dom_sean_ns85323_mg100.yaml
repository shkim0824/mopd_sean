# PHASE 4 (fork session) 2026-09-21, user request: branch of the main session's mopd-5dom-128-sean-merged-ns85323 at checkpoint-100.
# ONE merge at step 100 (tau 0.3, med 0.2, law 0.1, fin -, if 0.2), then plain MOPD to step 200.
# Sibling of p4_5dom_sean_ns85323_mg50.yaml (same merge at step 50), so no-merge / merge@50 /
# merge@100 can be compared at ck150 and ck200. Same teachers, including models/teacher-tau-sean.
# 5-domain MOPD, SEAN tau2 teacher, ns85323 merged init (tau=0.8,med=0.5,law=0.3,fin=0.2,if=0.3).
model:
  student: models/merged-5teachers-sean-ns85323
  dtype: float32
teachers:
  endpoints_file: ''
  top_k: 64
  max_workers: 64
  timeout: 900
data:
  paths:
    tau: data/rl_pools/tau_opd_train.jsonl
    med: data/rl_pools/med_sftp_train.jsonl
    law: data/rl_pools/law_rl_v2_train.jsonl
    fin: data/rl_pools/fin_train.jsonl
    if: data/rl_pools/if_train.jsonl
  max_rows_per_domain: null
  seed: 42
  thinking: true
  per_domain_per_batch: equal
  max_prompt_tokens: 15872
distill:
  mode: pg
  a_max: 5.0
  task_reward_coef: 0.0
  log_task_reward: false
train:
  lr: 1.0e-06
  batch_size: 128
  num_generations: 1
  max_completion_length: 16384
  max_steps: 200
  per_device_bs: 1
  temperature: 1.0
  top_p: 1.0
  beta: 0.0
  scheduler: constant
  warmup_ratio: 0.05
  max_grad_norm: 1.0
  save_steps: 25
  save_total_limit: null
  logging_steps: 1
  vllm_gpu_memory_utilization: 0.25
  vllm_tp: 1
  vllm_is_mode: token_truncate
  ddp_timeout: 7200
  mask_truncated_completions: true
  output: outputs/opd/p4-mopd-5dom-sean-ns85323-mg100-t3m2l1i2
